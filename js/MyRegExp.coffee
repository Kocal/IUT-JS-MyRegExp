class MyRegExp

    constructor: ->
        console.log "MyRegExp::constructor()"
        @_debug = true

        return


    isValid: (regex) ->
        console.log "MyRegExp::isValid" if @_debug

        return

    test: (text, regex) ->
        console.log "MyRegExp::test" if @_debug

        return

    exec: (text, regex) ->
        console.log "MyRegExp::exec" if @_debug

        return

